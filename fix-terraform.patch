--- Jenkinsfile.backup
+++ Jenkinsfile.fixed
@@ -[find line number]@@
                 sh '''
                     echo "Applying Terraform configuration..."
-                    terraform apply -auto-approve -var=environment=${params.ENVIRONMENT} -var=project_name=${APP_NAME} -var=docker_image=${DOCKER_IMAGE}:${DOCKER_TAG} tfplan.${BUILD_NUMBER}
+                    terraform apply -auto-approve tfplan.${BUILD_NUMBER}
                 '''
             }
         }
